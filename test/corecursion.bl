
def selfrec(i:Int):
    return unless i > 0
    $printf("Selfrec %ld\n", i)
    selfrec(i-1)

selfrec(4)

def ping(i:Int):
    return unless i > 0
    $printf("Ping %ld\n", i)
    pong(i-1)

def pong(i:Int):
    return unless i > 0
    $printf("Pong %ld\n", i)
    ping(i-1)

ping(5)
