foo := 12345
baz := "Hello $(2 + 3)"
str := "string"
extern random():Int
rand := random()

say "wip loaded!"

def bloop(s:String) = "bloop $s"

struct CustomStruct{x:Int, y:Int}

export foo, baz, rand, bloop, str, CustomStruct
