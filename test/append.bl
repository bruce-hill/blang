
def make_list(n:Int):[Int]
    tmp := [:Int]
    for i in 1..n
        tmp.append(i)
    return tmp

say "$(make_list(100))"

def make_string(n:Int):String
    tmp := ""
    for i in 1..n
        tmp += "$i"
    between tmp += ","
    return tmp

say(make_string 100)
