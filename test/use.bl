say "'use' is temporarily disabled"
// say "Loading wip..."
// x := use "modules/testmod"
// say "Fully loaded: $(x.rand)"
// say "Loading wip again..."
// y := use "modules/testmod"
// say "Fully loaded: $(y.rand)"
// fail "Module idempotency failed" unless x.rand == y.rand

// fail "Module string equality check failed '$(x.str)' != 'string'" unless x.str == "string"

// "blop" | (x.bloop) | say

// use "modules/testothermod"

// say "$from_othermod"
// say "Call: $(othermod_foo(5))"

// emp := use "modules/emptymod"
// say "$emp"
